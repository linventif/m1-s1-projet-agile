<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
  version="3.0">

  <persistence-unit name="default" transaction-type="RESOURCE_LOCAL">
    <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

    <class>fr.univ.m1.projetagile.core.domain.Vehicule</class>

    <properties>
      <!-- Oracle Database Connection -->
      <property name="jakarta.persistence.jdbc.driver" value="oracle.jdbc.OracleDriver" />
      <property name="jakarta.persistence.jdbc.url"
        value="jdbc:oracle:thin:@${ORACLE_HOST}:${ORACLE_PORT}/${ORACLE_SERVICE}" />
      <property name="jakarta.persistence.jdbc.user" value="${ORACLE_USER}" />
      <property name="jakarta.persistence.jdbc.password" value="${ORACLE_USER_PASSWORD}" />

      <!-- Hibernate Properties -->
      <property name="hibernate.dialect" value="org.hibernate.dialect.OracleDialect" />
      <!-- none = pas de validation, beaucoup plus rapide au démarrage -->
      <!-- update = valide et modifie le schéma (lent ~10s) -->
      <!-- validate = valide sans modifier (moyen ~3s) -->
      <property name="hibernate.hbm2ddl.auto" value="none" />
      <property name="hibernate.show_sql" value="false" />
      <property name="hibernate.format_sql" value="false" />

      <!-- Optimisations de performance -->
      <property name="hibernate.jdbc.batch_size" value="20" />
      <property name="hibernate.order_inserts" value="true" />
      <property name="hibernate.order_updates" value="true" />
      <property name="hibernate.jdbc.fetch_size" value="50" />
    </properties>
  </persistence-unit>
</persistence>
